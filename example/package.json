{
  "main": "buildscript.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "esbuild": "^0.8.1",
    "esbuild-svelte": "0.0.4"
  },
  "private": true
}
